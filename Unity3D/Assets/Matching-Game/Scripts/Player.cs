using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Player : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Temporary inputs to ensure game works properly
    void Update()
    {
        if (Input.GetKey(KeyCode.Q)) //Represents Left Match
        {

        }

        if (Input.GetKey(KeyCode.E)) //Represents Middle Match
        {

        }

        if (Input.GetKey(KeyCode.R)) //Represents Right Match
        {

        }
    }
}
